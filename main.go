package main

import (
	//"bufio"
	//"os"
	//"strings"
	//"fmt"
)

func main(){
	/*
	scanner := bufio.NewReader(os.Stdin)

	fmt.Print("Ringname: ")
	ringname, _ := scanner.ReadString('\n')

	fmt.Print("Alignment (face/heel): ")
	alignment, _ := scanner.ReadString('\n')

	fmt.Print("Signature Move: ")
	signature_move, _ := scanner.ReadString('\n')

	addWrestler(Wrestler{
		ringname: strings.TrimSpace(ringname),
		alignment: strings.TrimSpace(alignment),
		signature_move: strings.TrimSpace(signature_move),
	})
	
	listWrestlers()
	*/
	connect()
}